{
	"name": "<%= user_name %>",
	"dependencies": {
		"jquery": "<%= jqueryversion ? '~' + jqueryversion : 'latest' %>",
		<% if (fontawesome) { %> "font-awesome": "latest",<% } %>
		"modernizr": "latest",
		"normalize-css": "latest"
	}
}